import React from 'react';
import {Text, View} from "react-native";

const HomeScreen = () => {
    return (
        <Text>
            Search page
        </Text>
    );
};

export default HomeScreen;
