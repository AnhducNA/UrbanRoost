import React from 'react';
import {Text, View} from "react-native";

const FilterScreen = () => {
    return (
        <Text>
            Filter page
        </Text>
    );
};

export default FilterScreen;
