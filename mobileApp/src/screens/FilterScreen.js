import React from 'react';
import {Text, View} from "react-native";

const HomeScreen = () => {
    return (
        <Text>
            Filter page
        </Text>
    );
};

export default HomeScreen;
